apiVersion: v1
kind: ConfigMap
metadata:
  name: production-env
data:
  PRODUCTION.env: |
    root-redirect=/red/
    cors=http://localhost,http://localhost:1234,http://localhost:7777,http://preview.jvm.com,https://www-e.tech.visualvest.de,http://127.0.0.1:1234,https://34.118.17.82.nip.io,http://conversation-vv.productdock.com

    # ARANGO_DB 
    ARANGO_HOST=http://arango:8529
    ARANGO_USER=conversationuser
    ARANGO_PASSWORD=secret
    ARANGO_DATABASE=conversation

    ADMIN_USER=admin
    ADMIN_PASSWORD=secret
    JWT_EXPIRATION_SECONDS=300s

    # NODERED related configs

    USE_BUNDLED_NODERED=true
    NODERED_WS_CONNECTION=ws://localhost:8080
    #NODERED_HOME_DIR=/data
    NODERED_ENABLE_PROJECTS=true
